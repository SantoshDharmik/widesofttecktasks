<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Basic Form</title>
<style>
    body {
      font-family: Arial, sans-serif;
      background: #477eed;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .form-style {
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      width: 600px;
    }

    .form-style h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    .form-style label {
      font-weight: bold;
      margin-bottom: 5px;
      display: block;
      color: #444;
    }

    .form-style input,
    .form-style textarea,
    .form-style select {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 14px;
      transition: 0.3s;
    }

    .form-style input:focus,
    .form-style textarea:focus,
    .form-style select:focus {
      border-color: #007bff;
      outline: none;
      box-shadow: 0 0 5px rgba(0, 123, 255, 0.4);
    }

    .form-style button {
      width: 100%;
      padding: 12px;
      background: #007bff;
      color: #fff;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .form-style button:hover {
      background: #0056b3;
    }

    .form-style .hobbies {
      display: flex;
      gap: 10px;
      margin-bottom: 15px;
    }

    .form-style .hobbies label {
      font-weight: normal;
      color: #555;
    }
  </style>
</head>
<body>
    <!-- <h5>hello im Header code </h5> -->